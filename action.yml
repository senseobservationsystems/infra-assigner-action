name: 'Problem Assigned'
description: 'Problem Assignee Recommendation'
inputs:
  token:
    description: 'Github Token'
    required: true
outputs:
  time:
    description: 'count'
runs:
  using: 'node12'
  main: 'index.js'